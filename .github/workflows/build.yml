name: Build UX-Proto image

# Builds the UX-Proto image and pushes it to ghcr.io/<owner>/aw-app-ux-proto.
# Push builds run only for release bump commits so `latest` follows the tagged
# manifest release. Manual rebuilds remain available via workflow_dispatch.
#
#   gh workflow run build.yml                          # amd64 + linux/arm64 (default)
#   gh workflow run build.yml -f include_arm64=false   # amd64 only (skip QEMU arm64)
#
# One-time manual step after the FIRST push: the package is created private
# regardless of this repo's own visibility, and GitHub has no REST/GraphQL
# endpoint to change a container package's visibility (see aw-app-browser's
# build.yml for the confirmed 2026-07-28 finding). This blocks the "public
# app = tokenless install" policy (aw-workspace-apps-public-policy) until
# someone with org admin flips it by hand:
#   github.com/orgs/tekflox/packages/container/aw-app-ux-proto/settings
#   -> Package settings -> Danger Zone -> Change visibility -> Public
on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      include_arm64:
        description: 'Also build linux/arm64 (QEMU emulation; slower)'
        type: boolean
        default: true

concurrency:
  group: build-aw-app-ux-proto-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && contains(github.event.head_commit.message, '[skip release]')) }}
    name: aw-app-ux-proto
    runs-on: [self-hosted, aw-baremetal]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ghcr.io rejects mixed-case namespaces — lowercase the owner login.
      - name: Lowercase repo owner
        id: owner
        run: echo "lc=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - name: Resolve platforms
        id: plat
        run: |
          if [ "${{ inputs.include_arm64 }}" = "true" ]; then
            echo "platforms=linux/amd64,linux/arm64" >> "$GITHUB_OUTPUT"
          else
            echo "platforms=linux/amd64" >> "$GITHUB_OUTPUT"
          fi

      - name: Read app version
        id: version
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          print(f"version={json.load(open('aw-app.json'))['version']}")
          PY

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: container
          file: container/Dockerfile
          platforms: ${{ steps.plat.outputs.platforms }}
          push: true
          tags: |
            ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:latest
            ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:v${{ steps.version.outputs.version }}
            ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:${{ github.sha }}
          cache-from: type=registry,ref=ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:buildcache,mode=max

      - name: Summary
        run: |
          {
            echo "## 🐳 aw-app-ux-proto pushed"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| **Image** | \`ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:latest\` |"
            echo "| **Version tag** | \`ghcr.io/${{ steps.owner.outputs.lc }}/aw-app-ux-proto:v${{ steps.version.outputs.version }}\` |"
            echo "| **Platforms** | ${{ steps.plat.outputs.platforms }} |"
          } >> "$GITHUB_STEP_SUMMARY"
